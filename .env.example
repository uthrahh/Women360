# Base URL of the Women360 API (see server/README.md). Includes the /api/v1
# prefix so service calls can use clean relative paths like "/cycle/summary".
VITE_API_BASE_URL=http://localhost:4000/api/v1
